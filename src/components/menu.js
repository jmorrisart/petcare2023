import React, { Component, useState } from 'react';

const Menu = () => {
    // const [isActive, setIsActive] = useState(false);
    return (
        <React.Fragment>
            <div className="menu">
            </div>
        </React.Fragment>
    )
}

export default Menu;